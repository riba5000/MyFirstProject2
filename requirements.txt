httpx>=0.27.0
pydantic>=2.7.0
python-decouple>=3.8
APScheduler>=3.10.0
matplotlib>=3.9.0

# Fonte padrão (FONTE=gfsearch): Google Flights via SSR, sem chave e sem cota.
# O módulo importado chama-se `gf_search`.
google-flights-search>=0.1.21
